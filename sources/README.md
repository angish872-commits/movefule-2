# Frozen Source Snapshots

This directory is for comparison inputs only.

Do not put canonical production changes here.

Each source import must:

1. be frozen before analysis;
2. record a commit SHA or archive SHA-256 in `docs/reconciliation/01_SOURCE_REGISTRY.md`;
3. preserve original paths where practical;
4. remain unmodified during comparison;
5. be referenced by exact path in algorithm/database reconciliation records.

The current verified main application baseline remains `../Mufil-2/`.

The G/latest ZIP will be added here only after it is supplied and fingerprinted.
