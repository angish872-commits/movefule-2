diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_001_ReadinessCheckIn.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_001_ReadinessCheckIn.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_001_ReadinessCheckIn.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY001ReadinessCheckInScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_001",
+            title = "Readiness Check In",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Check In in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_002),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_010)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY001ReadinessCheckInScreenPreview() = MoveFuelTheme { RDY001ReadinessCheckInScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_002_EnergyCheck.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_002_EnergyCheck.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_002_EnergyCheck.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY002EnergyCheckScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_002",
+            title = "Energy Check",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Energy Check in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_003),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_001)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY002EnergyCheckScreenPreview() = MoveFuelTheme { RDY002EnergyCheckScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_003_FatigueCheck.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_003_FatigueCheck.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_003_FatigueCheck.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY003FatigueCheckScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_003",
+            title = "Fatigue Check",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Fatigue Check in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_004),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_002)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY003FatigueCheckScreenPreview() = MoveFuelTheme { RDY003FatigueCheckScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_004_RecoverySleepCheck.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_004_RecoverySleepCheck.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_004_RecoverySleepCheck.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY004RecoverySleepCheckScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_004",
+            title = "Recovery Sleep Check",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Recovery Sleep Check in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_005),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_003)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY004RecoverySleepCheckScreenPreview() = MoveFuelTheme { RDY004RecoverySleepCheckScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_005_SorenessCheck.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_005_SorenessCheck.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_005_SorenessCheck.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY005SorenessCheckScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_005",
+            title = "Soreness Check",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Soreness Check in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_006),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_004)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY005SorenessCheckScreenPreview() = MoveFuelTheme { RDY005SorenessCheckScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_006_EvaluatingReadiness.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_006_EvaluatingReadiness.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_006_EvaluatingReadiness.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY006EvaluatingReadinessScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_006",
+            title = "Evaluating Readiness",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Evaluating Readiness in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_007),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_005)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY006EvaluatingReadinessScreenPreview() = MoveFuelTheme { RDY006EvaluatingReadinessScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_007_ReadinessFull.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_007_ReadinessFull.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_007_ReadinessFull.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY007ReadinessFullScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_007",
+            title = "Readiness Full",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Full in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_008),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_006)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY007ReadinessFullScreenPreview() = MoveFuelTheme { RDY007ReadinessFullScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_008_ReadinessReduced.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_008_ReadinessReduced.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_008_ReadinessReduced.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY008ReadinessReducedScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_008",
+            title = "Readiness Reduced",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Reduced in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_009),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_007)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY008ReadinessReducedScreenPreview() = MoveFuelTheme { RDY008ReadinessReducedScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_009_ReadinessRecovery.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_009_ReadinessRecovery.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_009_ReadinessRecovery.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY009ReadinessRecoveryScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_009",
+            title = "Readiness Recovery",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Recovery in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_010),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_008)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY009ReadinessRecoveryScreenPreview() = MoveFuelTheme { RDY009ReadinessRecoveryScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_010_ReadinessStop.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_010_ReadinessStop.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/rdy/RDY_010_ReadinessStop.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.rdy
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun RDY010ReadinessStopScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "RDY_010",
+            title = "Readiness Stop",
+            subtitle = "Readiness evaluation with explicit FULL, REDUCED, RECOVERY, and STOP outcomes.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Stop in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Readiness uses text + icon + state, not color alone; STOP never exposes a normal Start action."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.EXR_001),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RDY_009)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun RDY010ReadinessStopScreenPreview() = MoveFuelTheme { RDY010ReadinessStopScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_001_SorenessDashboard.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_001_SorenessDashboard.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_001_SorenessDashboard.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR001SorenessDashboardScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_001",
+            title = "Soreness Dashboard",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Soreness Dashboard in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_002),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_032)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR001SorenessDashboardScreenPreview() = MoveFuelTheme { SOR001SorenessDashboardScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_002_FrontBodyMap.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_002_FrontBodyMap.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_002_FrontBodyMap.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR002FrontBodyMapScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_002",
+            title = "Front Body Map",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Front Body Map in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_003),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_001)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR002FrontBodyMapScreenPreview() = MoveFuelTheme { SOR002FrontBodyMapScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_003_BackBodyMap.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_003_BackBodyMap.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_003_BackBodyMap.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR003BackBodyMapScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_003",
+            title = "Back Body Map",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Back Body Map in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_004),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_002)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR003BackBodyMapScreenPreview() = MoveFuelTheme { SOR003BackBodyMapScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_004_SelectRegion.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_004_SelectRegion.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_004_SelectRegion.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR004SelectRegionScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_004",
+            title = "Select Region",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Select Region in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_005),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_003)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR004SelectRegionScreenPreview() = MoveFuelTheme { SOR004SelectRegionScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_005_SorenessSeverity.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_005_SorenessSeverity.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_005_SorenessSeverity.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR005SorenessSeverityScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_005",
+            title = "Soreness Severity",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Soreness Severity in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_006),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_004)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR005SorenessSeverityScreenPreview() = MoveFuelTheme { SOR005SorenessSeverityScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_006_MultipleRegionReview.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_006_MultipleRegionReview.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_006_MultipleRegionReview.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR006MultipleRegionReviewScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_006",
+            title = "Multiple Region Review",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Multiple Region Review in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_007),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_005)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR006MultipleRegionReviewScreenPreview() = MoveFuelTheme { SOR006MultipleRegionReviewScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_007_SaveSoreness.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_007_SaveSoreness.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_007_SaveSoreness.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR007SaveSorenessScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_007",
+            title = "Save Soreness",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Save Soreness in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_008),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_006)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR007SaveSorenessScreenPreview() = MoveFuelTheme { SOR007SaveSorenessScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_008_CurrentSorenessSummary.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_008_CurrentSorenessSummary.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_008_CurrentSorenessSummary.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR008CurrentSorenessSummaryScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_008",
+            title = "Current Soreness Summary",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Current Soreness Summary in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_009),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_007)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR008CurrentSorenessSummaryScreenPreview() = MoveFuelTheme { SOR008CurrentSorenessSummaryScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_009_SorenessHistory.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_009_SorenessHistory.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_009_SorenessHistory.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR009SorenessHistoryScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_009",
+            title = "Soreness History",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Soreness History in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_010),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_008)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR009SorenessHistoryScreenPreview() = MoveFuelTheme { SOR009SorenessHistoryScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_010_PainSafetyRoute.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_010_PainSafetyRoute.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/sor/SOR_010_PainSafetyRoute.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.sor
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun SOR010PainSafetyRouteScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "SOR_010",
+            title = "Pain Safety Route",
+            subtitle = "Soreness body-map workflow; pain is routed separately and is never silently auto-cleared.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Pain Safety Route in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Pain is not ordinary soreness. Soreness modifiers may expire, while pain requires explicit safety handling."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.RDY_001),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.SOR_009)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun SOR010PainSafetyRouteScreenPreview() = MoveFuelTheme { SOR010PainSafetyRouteScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_001_TrainNowDashboard.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_001_TrainNowDashboard.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_001_TrainNowDashboard.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN001TrainNowDashboardScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_001",
+            title = "Train Now Dashboard",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Now Dashboard in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_002),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_020)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN001TrainNowDashboardScreenPreview() = MoveFuelTheme { TRN001TrainNowDashboardScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_002_ReadinessSummary.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_002_ReadinessSummary.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_002_ReadinessSummary.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN002ReadinessSummaryScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_002",
+            title = "Readiness Summary",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Summary in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_003),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_001)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN002ReadinessSummaryScreenPreview() = MoveFuelTheme { TRN002ReadinessSummaryScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_003_ReadinessDetail.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_003_ReadinessDetail.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_003_ReadinessDetail.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN003ReadinessDetailScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_003",
+            title = "Readiness Detail",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Detail in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_004),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_002)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN003ReadinessDetailScreenPreview() = MoveFuelTheme { TRN003ReadinessDetailScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_004_WhyAdapted.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_004_WhyAdapted.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_004_WhyAdapted.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN004WhyAdaptedScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_004",
+            title = "Why Adapted",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Why Adapted in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_005),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_003)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN004WhyAdaptedScreenPreview() = MoveFuelTheme { TRN004WhyAdaptedScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_005_TodayWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_005_TodayWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_005_TodayWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN005TodayWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_005",
+            title = "Today Workout",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Today Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_006),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_004)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN005TodayWorkoutScreenPreview() = MoveFuelTheme { TRN005TodayWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_006_WorkoutDetail.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_006_WorkoutDetail.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_006_WorkoutDetail.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN006WorkoutDetailScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_006",
+            title = "Workout Detail",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Workout Detail in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_007),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_005)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN006WorkoutDetailScreenPreview() = MoveFuelTheme { TRN006WorkoutDetailScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_007_StartWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_007_StartWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_007_StartWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN007StartWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_007",
+            title = "Start Workout",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Start Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_008),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_006)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN007StartWorkoutScreenPreview() = MoveFuelTheme { TRN007StartWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_008_RescheduleWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_008_RescheduleWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_008_RescheduleWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN008RescheduleWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_008",
+            title = "Reschedule Workout",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Reschedule Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_009),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_007)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN008RescheduleWorkoutScreenPreview() = MoveFuelTheme { TRN008RescheduleWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_009_QuickStart.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_009_QuickStart.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_009_QuickStart.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN009QuickStartScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_009",
+            title = "Quick Start",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Quick Start in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_010),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_008)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN009QuickStartScreenPreview() = MoveFuelTheme { TRN009QuickStartScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_010_WeeklyPlan.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_010_WeeklyPlan.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_010_WeeklyPlan.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN010WeeklyPlanScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_010",
+            title = "Weekly Plan",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Weekly Plan in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_011),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_009)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN010WeeklyPlanScreenPreview() = MoveFuelTheme { TRN010WeeklyPlanScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_011_WeeklyDayDetail.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_011_WeeklyDayDetail.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_011_WeeklyDayDetail.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN011WeeklyDayDetailScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_011",
+            title = "Weekly Day Detail",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Weekly Day Detail in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_012),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_010)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN011WeeklyDayDetailScreenPreview() = MoveFuelTheme { TRN011WeeklyDayDetailScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_012_LessTimeToday.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_012_LessTimeToday.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_012_LessTimeToday.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN012LessTimeTodayScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_012",
+            title = "Less Time Today",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Less Time Today in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_013),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_011)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN012LessTimeTodayScreenPreview() = MoveFuelTheme { TRN012LessTimeTodayScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_013_EquipmentUnavailable.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_013_EquipmentUnavailable.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_013_EquipmentUnavailable.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN013EquipmentUnavailableScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_013",
+            title = "Equipment Unavailable",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Equipment Unavailable in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_014),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_012)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN013EquipmentUnavailableScreenPreview() = MoveFuelTheme { TRN013EquipmentUnavailableScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_014_ReplacementOptions.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_014_ReplacementOptions.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_014_ReplacementOptions.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN014ReplacementOptionsScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_014",
+            title = "Replacement Options",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Replacement Options in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_015),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_013)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN014ReplacementOptionsScreenPreview() = MoveFuelTheme { TRN014ReplacementOptionsScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_015_RecoveryDay.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_015_RecoveryDay.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_015_RecoveryDay.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN015RecoveryDayScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_015",
+            title = "Recovery Day",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Recovery Day in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_016),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_014)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN015RecoveryDayScreenPreview() = MoveFuelTheme { TRN015RecoveryDayScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_016_MissedWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_016_MissedWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_016_MissedWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN016MissedWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_016",
+            title = "Missed Workout",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Missed Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_017),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_015)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN016MissedWorkoutScreenPreview() = MoveFuelTheme { TRN016MissedWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_017_ProgramSettings.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_017_ProgramSettings.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_017_ProgramSettings.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN017ProgramSettingsScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_017",
+            title = "Program Settings",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Program Settings in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_018),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_016)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN017ProgramSettingsScreenPreview() = MoveFuelTheme { TRN017ProgramSettingsScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_018_ProgramRevisionHistory.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_018_ProgramRevisionHistory.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_018_ProgramRevisionHistory.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN018ProgramRevisionHistoryScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_018",
+            title = "Program Revision History",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Program Revision History in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_019),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_017)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN018ProgramRevisionHistoryScreenPreview() = MoveFuelTheme { TRN018ProgramRevisionHistoryScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_019_RecentWorkouts.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_019_RecentWorkouts.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_019_RecentWorkouts.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN019RecentWorkoutsScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_019",
+            title = "Recent Workouts",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Recent Workouts in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_020),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_018)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN019RecentWorkoutsScreenPreview() = MoveFuelTheme { TRN019RecentWorkoutsScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_020_TrainLoading.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_020_TrainLoading.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_020_TrainLoading.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN020TrainLoadingScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_020",
+            title = "Train Loading",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Loading in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_021),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_019)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN020TrainLoadingScreenPreview() = MoveFuelTheme { TRN020TrainLoadingScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_021_TrainEmpty.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_021_TrainEmpty.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_021_TrainEmpty.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN021TrainEmptyScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_021",
+            title = "Train Empty",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Empty in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_022),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_020)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN021TrainEmptyScreenPreview() = MoveFuelTheme { TRN021TrainEmptyScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_022_TrainUnknown.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_022_TrainUnknown.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_022_TrainUnknown.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN022TrainUnknownScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_022",
+            title = "Train Unknown",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Unknown in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_023),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_021)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN022TrainUnknownScreenPreview() = MoveFuelTheme { TRN022TrainUnknownScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_023_TrainOffline.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_023_TrainOffline.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_023_TrainOffline.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN023TrainOfflineScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_023",
+            title = "Train Offline",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Offline in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_024),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_022)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN023TrainOfflineScreenPreview() = MoveFuelTheme { TRN023TrainOfflineScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_024_TrainStale.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_024_TrainStale.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trn/TRN_024_TrainStale.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trn
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRN024TrainStaleScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRN_024",
+            title = "Train Stale",
+            subtitle = "The training workspace for readiness, today’s workout, adaptations, schedule, and program context.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Stale in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_001),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_023)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRN024TrainStaleScreenPreview() = MoveFuelTheme { TRN024TrainStaleScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_001_TrainSetupWelcome.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_001_TrainSetupWelcome.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_001_TrainSetupWelcome.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS001TrainSetupWelcomeScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_001",
+            title = "Train Setup Welcome",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Train Setup Welcome in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_002),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.RCP_036)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS001TrainSetupWelcomeScreenPreview() = MoveFuelTheme { TRS001TrainSetupWelcomeScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_002_TrainingExperience.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_002_TrainingExperience.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_002_TrainingExperience.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS002TrainingExperienceScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_002",
+            title = "Training Experience",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Training Experience in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_003),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_001)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS002TrainingExperienceScreenPreview() = MoveFuelTheme { TRS002TrainingExperienceScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_003_ActivityExperience.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_003_ActivityExperience.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_003_ActivityExperience.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS003ActivityExperienceScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_003",
+            title = "Activity Experience",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Activity Experience in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_004),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_002)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS003ActivityExperienceScreenPreview() = MoveFuelTheme { TRS003ActivityExperienceScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_004_TrainingGoal.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_004_TrainingGoal.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_004_TrainingGoal.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS004TrainingGoalScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_004",
+            title = "Training Goal",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Training Goal in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_005),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_003)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS004TrainingGoalScreenPreview() = MoveFuelTheme { TRS004TrainingGoalScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_005_PreferredActivities.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_005_PreferredActivities.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_005_PreferredActivities.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS005PreferredActivitiesScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_005",
+            title = "Preferred Activities",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Preferred Activities in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_006),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_004)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS005PreferredActivitiesScreenPreview() = MoveFuelTheme { TRS005PreferredActivitiesScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_006_Environment.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_006_Environment.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_006_Environment.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS006EnvironmentScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_006",
+            title = "Environment",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Environment in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_007),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_005)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS006EnvironmentScreenPreview() = MoveFuelTheme { TRS006EnvironmentScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_007_Equipment.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_007_Equipment.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_007_Equipment.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS007EquipmentScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_007",
+            title = "Equipment",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Equipment in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_008),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_006)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS007EquipmentScreenPreview() = MoveFuelTheme { TRS007EquipmentScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_008_WeeklyAvailability.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_008_WeeklyAvailability.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_008_WeeklyAvailability.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS008WeeklyAvailabilityScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_008",
+            title = "Weekly Availability",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Weekly Availability in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_009),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_007)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS008WeeklyAvailabilityScreenPreview() = MoveFuelTheme { TRS008WeeklyAvailabilityScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_009_PreferredDays.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_009_PreferredDays.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_009_PreferredDays.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS009PreferredDaysScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_009",
+            title = "Preferred Days",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Preferred Days in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_010),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_008)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS009PreferredDaysScreenPreview() = MoveFuelTheme { TRS009PreferredDaysScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_010_PreferredTime.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_010_PreferredTime.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_010_PreferredTime.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS010PreferredTimeScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_010",
+            title = "Preferred Time",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Preferred Time in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_011),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_009)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS010PreferredTimeScreenPreview() = MoveFuelTheme { TRS010PreferredTimeScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_011_SessionDuration.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_011_SessionDuration.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_011_SessionDuration.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS011SessionDurationScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_011",
+            title = "Session Duration",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Session Duration in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_012),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_010)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS011SessionDurationScreenPreview() = MoveFuelTheme { TRS011SessionDurationScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_012_ExercisePreferences.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_012_ExercisePreferences.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_012_ExercisePreferences.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS012ExercisePreferencesScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_012",
+            title = "Exercise Preferences",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Exercise Preferences in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_013),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_011)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS012ExercisePreferencesScreenPreview() = MoveFuelTheme { TRS012ExercisePreferencesScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_013_ExerciseDislikes.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_013_ExerciseDislikes.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_013_ExerciseDislikes.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS013ExerciseDislikesScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_013",
+            title = "Exercise Dislikes",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Exercise Dislikes in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_014),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_012)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS013ExerciseDislikesScreenPreview() = MoveFuelTheme { TRS013ExerciseDislikesScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_014_MovementLimitations.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_014_MovementLimitations.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_014_MovementLimitations.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS014MovementLimitationsScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_014",
+            title = "Movement Limitations",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Movement Limitations in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_015),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_013)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS014MovementLimitationsScreenPreview() = MoveFuelTheme { TRS014MovementLimitationsScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_015_LongTermRestrictions.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_015_LongTermRestrictions.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_015_LongTermRestrictions.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS015LongTermRestrictionsScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_015",
+            title = "Long Term Restrictions",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Long Term Restrictions in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_016),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_014)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS015LongTermRestrictionsScreenPreview() = MoveFuelTheme { TRS015LongTermRestrictionsScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_016_PainSafety.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_016_PainSafety.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_016_PainSafety.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS016PainSafetyScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_016",
+            title = "Pain Safety",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Pain Safety in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_017),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_015)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS016PainSafetyScreenPreview() = MoveFuelTheme { TRS016PainSafetyScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_017_SorenessSetup.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_017_SorenessSetup.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_017_SorenessSetup.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS017SorenessSetupScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_017",
+            title = "Soreness Setup",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Soreness Setup in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_018),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_016)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS017SorenessSetupScreenPreview() = MoveFuelTheme { TRS017SorenessSetupScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_018_ReadinessSetup.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_018_ReadinessSetup.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_018_ReadinessSetup.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS018ReadinessSetupScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_018",
+            title = "Readiness Setup",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Readiness Setup in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_019),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_017)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS018ReadinessSetupScreenPreview() = MoveFuelTheme { TRS018ReadinessSetupScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_019_PerformanceBaseline.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_019_PerformanceBaseline.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_019_PerformanceBaseline.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS019PerformanceBaselineScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_019",
+            title = "Performance Baseline",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Performance Baseline in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRS_020),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_018)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS019PerformanceBaselineScreenPreview() = MoveFuelTheme { TRS019PerformanceBaselineScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_020_PlanPreview.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_020_PlanPreview.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/trs/TRS_020_PlanPreview.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.trs
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun TRS020PlanPreviewScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "TRS_020",
+            title = "Plan Preview",
+            subtitle = "First-time training setup collects only inputs needed to build a safe, understandable plan.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Plan Preview in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Training UI renders state and emits actions; algorithms remain outside dashboard files."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.TRN_001),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRS_019)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun TRS020PlanPreviewScreenPreview() = MoveFuelTheme { TRS020PlanPreviewScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_001_PreWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_001_PreWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_001_PreWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK001PreWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_001",
+            title = "Pre Workout",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Pre Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_002),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.TRN_024)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK001PreWorkoutScreenPreview() = MoveFuelTheme { WRK001PreWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_002_WorkoutStart.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_002_WorkoutStart.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_002_WorkoutStart.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK002WorkoutStartScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_002",
+            title = "Workout Start",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Workout Start in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_003),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_001)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK002WorkoutStartScreenPreview() = MoveFuelTheme { WRK002WorkoutStartScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_003_ActiveExercise.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_003_ActiveExercise.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_003_ActiveExercise.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK003ActiveExerciseScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_003",
+            title = "Active Exercise",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Active Exercise in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_004),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_002)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK003ActiveExerciseScreenPreview() = MoveFuelTheme { WRK003ActiveExerciseScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_004_ExerciseMediaLoading.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_004_ExerciseMediaLoading.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_004_ExerciseMediaLoading.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK004ExerciseMediaLoadingScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_004",
+            title = "Exercise Media Loading",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Exercise Media Loading in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_005),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_003)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK004ExerciseMediaLoadingScreenPreview() = MoveFuelTheme { WRK004ExerciseMediaLoadingScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_005_ExerciseInstructions.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_005_ExerciseInstructions.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_005_ExerciseInstructions.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK005ExerciseInstructionsScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_005",
+            title = "Exercise Instructions",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Exercise Instructions in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_006),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_004)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK005ExerciseInstructionsScreenPreview() = MoveFuelTheme { WRK005ExerciseInstructionsScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_006_SetInput.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_006_SetInput.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_006_SetInput.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK006SetInputScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_006",
+            title = "Set Input",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Set Input in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_007),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_005)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK006SetInputScreenPreview() = MoveFuelTheme { WRK006SetInputScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_007_RepsInput.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_007_RepsInput.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_007_RepsInput.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK007RepsInputScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_007",
+            title = "Reps Input",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Reps Input in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_008),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_006)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK007RepsInputScreenPreview() = MoveFuelTheme { WRK007RepsInputScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_008_LoadInput.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_008_LoadInput.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_008_LoadInput.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK008LoadInputScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_008",
+            title = "Load Input",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Load Input in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_009),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_007)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK008LoadInputScreenPreview() = MoveFuelTheme { WRK008LoadInputScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_009_TimeInput.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_009_TimeInput.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_009_TimeInput.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK009TimeInputScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_009",
+            title = "Time Input",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Time Input in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_010),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_008)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK009TimeInputScreenPreview() = MoveFuelTheme { WRK009TimeInputScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_010_DistanceInput.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_010_DistanceInput.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_010_DistanceInput.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK010DistanceInputScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_010",
+            title = "Distance Input",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Distance Input in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_011),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_009)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK010DistanceInputScreenPreview() = MoveFuelTheme { WRK010DistanceInputScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_011_RPEInput.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_011_RPEInput.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_011_RPEInput.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK011RPEInputScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_011",
+            title = "RPE Input",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "RPE Input in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_012),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_010)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK011RPEInputScreenPreview() = MoveFuelTheme { WRK011RPEInputScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_012_ReviewSet.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_012_ReviewSet.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_012_ReviewSet.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK012ReviewSetScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_012",
+            title = "Review Set",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Review Set in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_013),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_011)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK012ReviewSetScreenPreview() = MoveFuelTheme { WRK012ReviewSetScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_013_CompleteSet.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_013_CompleteSet.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_013_CompleteSet.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK013CompleteSetScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_013",
+            title = "Complete Set",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Complete Set in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_014),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_012)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK013CompleteSetScreenPreview() = MoveFuelTheme { WRK013CompleteSetScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_014_RestTimer.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_014_RestTimer.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_014_RestTimer.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK014RestTimerScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_014",
+            title = "Rest Timer",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Rest Timer in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_015),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_013)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK014RestTimerScreenPreview() = MoveFuelTheme { WRK014RestTimerScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_015_ExtendRest.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_015_ExtendRest.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_015_ExtendRest.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK015ExtendRestScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_015",
+            title = "Extend Rest",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Extend Rest in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_016),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_014)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK015ExtendRestScreenPreview() = MoveFuelTheme { WRK015ExtendRestScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_016_SkipRest.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_016_SkipRest.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_016_SkipRest.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK016SkipRestScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_016",
+            title = "Skip Rest",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Skip Rest in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_017),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_015)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK016SkipRestScreenPreview() = MoveFuelTheme { WRK016SkipRestScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_017_NextSet.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_017_NextSet.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_017_NextSet.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK017NextSetScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_017",
+            title = "Next Set",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Next Set in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_018),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_016)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK017NextSetScreenPreview() = MoveFuelTheme { WRK017NextSetScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_018_ExerciseComplete.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_018_ExerciseComplete.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_018_ExerciseComplete.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK018ExerciseCompleteScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_018",
+            title = "Exercise Complete",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Exercise Complete in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_019),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_017)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK018ExerciseCompleteScreenPreview() = MoveFuelTheme { WRK018ExerciseCompleteScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_019_NextExercise.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_019_NextExercise.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_019_NextExercise.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK019NextExerciseScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_019",
+            title = "Next Exercise",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Next Exercise in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_020),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_018)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK019NextExerciseScreenPreview() = MoveFuelTheme { WRK019NextExerciseScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_020_SubstituteExercise.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_020_SubstituteExercise.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_020_SubstituteExercise.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK020SubstituteExerciseScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_020",
+            title = "Substitute Exercise",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Substitute Exercise in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_021),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_019)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK020SubstituteExerciseScreenPreview() = MoveFuelTheme { WRK020SubstituteExerciseScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_021_SubstitutionCandidates.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_021_SubstitutionCandidates.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_021_SubstitutionCandidates.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK021SubstitutionCandidatesScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_021",
+            title = "Substitution Candidates",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Substitution Candidates in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_022),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_020)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK021SubstitutionCandidatesScreenPreview() = MoveFuelTheme { WRK021SubstitutionCandidatesScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_022_SubstitutionDetail.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_022_SubstitutionDetail.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_022_SubstitutionDetail.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK022SubstitutionDetailScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_022",
+            title = "Substitution Detail",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Substitution Detail in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_023),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_021)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK022SubstitutionDetailScreenPreview() = MoveFuelTheme { WRK022SubstitutionDetailScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_023_ConfirmSubstitute.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_023_ConfirmSubstitute.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_023_ConfirmSubstitute.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK023ConfirmSubstituteScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_023",
+            title = "Confirm Substitute",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Confirm Substitute in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_024),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_022)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK023ConfirmSubstituteScreenPreview() = MoveFuelTheme { WRK023ConfirmSubstituteScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_024_SkipSet.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_024_SkipSet.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_024_SkipSet.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK024SkipSetScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_024",
+            title = "Skip Set",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Skip Set in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_025),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_023)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK024SkipSetScreenPreview() = MoveFuelTheme { WRK024SkipSetScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_025_SkipExercise.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_025_SkipExercise.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_025_SkipExercise.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK025SkipExerciseScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_025",
+            title = "Skip Exercise",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Skip Exercise in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_026),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_024)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK025SkipExerciseScreenPreview() = MoveFuelTheme { WRK025SkipExerciseScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_026_WorkoutNote.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_026_WorkoutNote.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_026_WorkoutNote.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK026WorkoutNoteScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_026",
+            title = "Workout Note",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Workout Note in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_027),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_025)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK026WorkoutNoteScreenPreview() = MoveFuelTheme { WRK026WorkoutNoteScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_027_PauseWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_027_PauseWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_027_PauseWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK027PauseWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_027",
+            title = "Pause Workout",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Pause Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_028),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_026)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK027PauseWorkoutScreenPreview() = MoveFuelTheme { WRK027PauseWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_028_ResumeWorkout.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_028_ResumeWorkout.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_028_ResumeWorkout.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK028ResumeWorkoutScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_028",
+            title = "Resume Workout",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Resume Workout in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_029),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_027)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK028ResumeWorkoutScreenPreview() = MoveFuelTheme { WRK028ResumeWorkoutScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_029_PainSafetyPause.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_029_PainSafetyPause.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_029_PainSafetyPause.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK029PainSafetyPauseScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_029",
+            title = "Pain Safety Pause",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Pain Safety Pause in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_030),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_028)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK029PainSafetyPauseScreenPreview() = MoveFuelTheme { WRK029PainSafetyPauseScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_030_FinishEarlyConfirmation.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_030_FinishEarlyConfirmation.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_030_FinishEarlyConfirmation.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK030FinishEarlyConfirmationScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_030",
+            title = "Finish Early Confirmation",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Finish Early Confirmation in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_031),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_029)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK030FinishEarlyConfirmationScreenPreview() = MoveFuelTheme { WRK030FinishEarlyConfirmationScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_031_PostWorkoutCheckIn.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_031_PostWorkoutCheckIn.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_031_PostWorkoutCheckIn.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK031PostWorkoutCheckInScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_031",
+            title = "Post Workout Check In",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Post Workout Check In in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.WRK_032),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_030)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK031PostWorkoutCheckInScreenPreview() = MoveFuelTheme { WRK031PostWorkoutCheckInScreen {} }
diff --git a/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_032_WorkoutSummary.kt b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_032_WorkoutSummary.kt
new file mode 100644
--- /dev/null
+++ b/Mufil-2/app/src/main/java/com/movefuel/mufil2/ui/screens/wrk/WRK_032_WorkoutSummary.kt
@@ -0,0 +1,33 @@
+package com.movefuel.mufil2.ui.screens.wrk
+
+import androidx.compose.runtime.Composable
+import androidx.compose.ui.tooling.preview.Preview
+import com.movefuel.mufil2.ui.components.MoveFuelDashboardScaffold
+import com.movefuel.mufil2.ui.design.MoveFuelTheme
+import com.movefuel.mufil2.ui.model.DashboardAction
+import com.movefuel.mufil2.ui.model.DashboardSection
+import com.movefuel.mufil2.ui.model.DashboardSpec
+import com.movefuel.mufil2.ui.navigation.MoveFuelRoute
+
+@Composable
+fun WRK032WorkoutSummaryScreen(onNavigate: (MoveFuelRoute) -> Unit) {
+    MoveFuelDashboardScaffold(
+        spec = DashboardSpec(
+            id = "WRK_032",
+            title = "Workout Summary",
+            subtitle = "Dedicated execution UI where prescribed values and actual performed values remain distinct.",
+            sections = listOf(
+                DashboardSection("Visible UI", "Workout Summary in the premium MoveFuel Sage system."),
+                DashboardSection("Training contract", "Actual reps, load, time, distance, and RPE are performed facts and never overwrite the prescription."),
+                DashboardSection("Motion", "Loading, buffering, timers, completion, pause, and transition states use shared motion components so the app never looks frozen."),
+            ),
+            primaryAction = DashboardAction("Continue", MoveFuelRoute.SOR_001),
+            secondaryActions = listOf(DashboardAction("Back", MoveFuelRoute.WRK_031)),
+        ),
+        onNavigate = onNavigate,
+    )
+}
+
+@Preview(showBackground = true, backgroundColor = 0xFF0E130F, widthDp = 390, heightDp = 844)
+@Composable
+private fun WRK032WorkoutSummaryScreenPreview() = MoveFuelTheme { WRK032WorkoutSummaryScreen {} }
